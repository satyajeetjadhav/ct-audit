import { MetaToEnglishPipe } from './meta-to-english.pipe';

describe('MetaToEnglishPipe', () => {
  it('create an instance', () => {
    const pipe = new MetaToEnglishPipe();
    expect(pipe).toBeTruthy();
  });
});
